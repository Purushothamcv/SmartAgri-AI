# 🌾 Smart Agri - Application Architecture

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                           │
│                     (React 18 + Vite)                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      AUTHENTICATION LAYER                       │
│                    (JWT + Context API)                         │
│  ┌──────────┐  ┌──────────┐  ┌────────────────┐              │
│  │  Login   │  │ Register │  │ Protected Route│              │
│  └──────────┘  └──────────┘  └────────────────┘              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       ROUTING LAYER                             │
│                   (React Router v6)                            │
│  10 Routes: Dashboard, 8 Modules, Auth Pages                  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     PRESENTATION LAYER                          │
│                  (Components + Pages)                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │  Dashboard  │  │   Modules   │  │ Components  │          │
│  │  - Map      │  │  - Crop     │  │  - Navbar   │          │
│  │  - Weather  │  │  - Yield    │  │  - Cards    │          │
│  │  - Nav      │  │  - Disease  │  │  - Forms    │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        API LAYER                                │
│                  (Axios + Services)                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │  Auth    │  │  Crop    │  │ Disease  │  │ Chatbot  │     │
│  │ Service  │  │ Service  │  │ Service  │  │ Service  │     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND API (Separate)                       │
│                  REST API Endpoints                            │
│                  http://localhost:8000/api                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

### User Journey: Crop Recommendation

```
┌─────────┐
│  USER   │
└────┬────┘
     │ 1. Visit site
     ▼
┌─────────────┐
│ Login Page  │ ◄─── Not Authenticated
└─────┬───────┘
      │ 2. Enter credentials
      ▼
┌─────────────────┐
│ AuthContext     │ ─── Stores JWT token
│ (Global State)  │ ─── User info
└────────┬────────┘
         │ 3. Authenticated
         ▼
┌────────────────┐
│   Dashboard    │
└────────┬───────┘
         │ 4. Click "Crop Recommendation"
         ▼
┌──────────────────────┐
│ CropRecommendation   │
│     Component        │
└──────────┬───────────┘
           │ 5. Fill form (N, P, K, etc.)
           ▼
┌──────────────────────┐
│   InputField         │ ◄─── Reusable component
│   Components         │
└──────────┬───────────┘
           │ 6. Submit form
           ▼
┌──────────────────────┐
│  cropService.        │
│  recommendCrop()     │
└──────────┬───────────┘
           │ 7. API call with JWT
           ▼
┌──────────────────────┐
│  Backend API         │
│  POST /crop/recommend│
└──────────┬───────────┘
           │ 8. ML Model prediction
           ▼
┌──────────────────────┐
│   Response           │
│   { crop: "Rice" }   │
└──────────┬───────────┘
           │ 9. Return to frontend
           ▼
┌──────────────────────┐
│   ResultCard         │ ◄─── Display result
│   Component          │
└──────────────────────┘
```

---

## 🗂️ Component Hierarchy

```
App
├── AuthProvider (Context)
│   └── Router
│       ├── Public Routes
│       │   ├── Login
│       │   └── Register
│       │
│       └── Protected Routes
│           ├── Dashboard
│           │   ├── Navbar
│           │   ├── MapContainer
│           │   │   └── LocationMarker
│           │   ├── WeatherCard
│           │   └── Module Cards
│           │
│           ├── CropRecommendation
│           │   ├── Navbar
│           │   ├── InputField (×8)
│           │   ├── LoadingSpinner
│           │   └── ResultCard
│           │
│           ├── YieldPrediction
│           │   ├── Navbar
│           │   ├── InputField (×6)
│           │   └── ResultCard
│           │
│           ├── FertilizerRecommendation
│           │   ├── Navbar
│           │   ├── InputField (×8)
│           │   └── ResultCard
│           │
│           ├── StressPrediction
│           │   ├── Navbar
│           │   ├── InputField (×6)
│           │   └── ResultCard
│           │
│           ├── BestSprayTime
│           │   ├── Navbar
│           │   ├── InputField (×5)
│           │   └── ResultCard
│           │
│           ├── FruitDisease
│           │   ├── Navbar
│           │   ├── ImageUploader
│           │   └── ResultCard
│           │
│           ├── LeafDisease
│           │   ├── Navbar
│           │   ├── ImageUploader
│           │   └── ResultCard
│           │
│           └── Chatbot
│               ├── Navbar
│               ├── ChatMessage (×N)
│               └── Input Area
```

---

## 📦 State Management Flow

```
┌──────────────────────────────────────────┐
│         AuthContext (Global)             │
│                                          │
│  State:                                  │
│  ├── user: { id, name, email }         │
│  ├── loading: boolean                   │
│  └── isAuthenticated: boolean           │
│                                          │
│  Methods:                                │
│  ├── login(credentials)                 │
│  ├── register(userData)                 │
│  └── logout()                            │
└──────────────────────────────────────────┘
              │
              ├─► ProtectedRoute (checks auth)
              ├─► Navbar (displays user)
              └─► All Pages (access user info)

┌──────────────────────────────────────────┐
│      Component Local State               │
│                                          │
│  Each Page Component:                    │
│  ├── formData: { ...inputs }           │
│  ├── result: null | object              │
│  ├── loading: boolean                   │
│  └── error: string                       │
└──────────────────────────────────────────┘
```

---

## 🌐 API Service Architecture

```
┌────────────────────────────────────────────────┐
│             api.js (Axios Instance)            │
│                                                │
│  Config:                                       │
│  ├── baseURL: VITE_API_BASE_URL               │
│  ├── headers: { Content-Type, Authorization } │
│  │                                             │
│  Request Interceptor:                         │
│  └── Add JWT token to all requests            │
│                                                │
│  Response Interceptor:                        │
│  └── Handle 401 → redirect to login           │
└────────────────────────────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────────┐
│          services.js (API Methods)             │
│                                                │
│  authService:                                  │
│  ├── login(credentials)                       │
│  ├── register(userData)                       │
│  └── logout()                                  │
│                                                │
│  weatherService:                               │
│  └── getWeather(lat, lon)                     │
│                                                │
│  cropService:                                  │
│  ├── recommendCrop(data)                      │
│  ├── predictYield(data)                       │
│  ├── predictStress(data)                      │
│  └── getBestSprayTime(data)                   │
│                                                │
│  fertilizerService:                            │
│  └── recommendFertilizer(data)                │
│                                                │
│  diseaseService:                               │
│  ├── classifyFruitDisease(formData)          │
│  └── detectLeafDisease(formData)             │
│                                                │
│  chatbotService:                               │
│  └── sendMessage(message)                     │
└────────────────────────────────────────────────┘
```

---

## 🎨 UI Component Breakdown

### Input Field Component
```
┌────────────────────────────────┐
│ Label                 * Required│
├────────────────────────────────┤
│ 🔍 │ [Input Field]            │
└────────────────────────────────┘

Props:
- label: string
- name: string
- type: text|number|email|password
- value: any
- onChange: function
- icon: Component (optional)
- required: boolean
```

### Weather Card
```
┌────────────────────────────────┐
│ ☁️  Current Weather            │
├────────────────────────────────┤
│  🌡️  Temperature    28°C       │
│  💧  Humidity       65%        │
│  🌧️  Rainfall       2mm        │
│  💨  Wind Speed     12 km/h    │
└────────────────────────────────┘

Props:
- weather: { temp, humidity, rainfall, wind }
```

### Result Card
```
┌────────────────────────────────┐
│ ✓  Success Title               │
├────────────────────────────────┤
│ Key 1:          Value 1        │
│ Key 2:          Value 2        │
│ Key 3:          Value 3        │
└────────────────────────────────┘

Props:
- result: object|string
- type: success|warning|error|info
- title: string
- icon: Component
```

### Image Uploader
```
┌────────────────────────────────┐
│ Upload Image                   │
├────────────────────────────────┤
│                                │
│        📤 Upload               │
│   Click or drag and drop       │
│                                │
└────────────────────────────────┘
        ↓ After upload
┌────────────────────────────────┐
│ [Image Preview]                │
│ filename.jpg              ❌   │
└────────────────────────────────┘

Props:
- onImageSelect: function
- label: string
- accept: string
```

---

## 🚀 Routing Structure

```
/
├── /login (Public)
├── /register (Public)
│
└── Protected Routes (requires auth)
    ├── /dashboard
    ├── /crop-recommendation
    ├── /yield-prediction
    ├── /fertilizer
    ├── /stress-prediction
    ├── /spray-time
    ├── /fruit-disease
    ├── /leaf-disease
    └── /chatbot

Default: / → redirects to /dashboard
404: * → redirects to /dashboard
```

---

## 🔐 Authentication Flow

```
User visits site
    │
    ▼
Is user authenticated?
    │
    ├─ YES ──► Load Dashboard
    │
    └─ NO ───► Redirect to /login
                    │
                    ▼
            User logs in
                    │
                    ▼
            POST /api/auth/login
                    │
                    ▼
            Receive JWT token
                    │
                    ▼
        Store in localStorage
                    │
                    ▼
        Update AuthContext
                    │
                    ▼
        Redirect to /dashboard
                    │
                    ▼
        All subsequent requests
        include JWT in headers
```

---

## 📊 Module Interaction Flow

### Example: Disease Detection Module

```
User → Upload Image
         │
         ▼
ImageUploader Component
         │
         ├─ Shows preview
         ├─ Stores file in state
         └─ Enables submit button
         │
         ▼
User → Clicks "Detect Disease"
         │
         ▼
Create FormData object
         │
         ▼
diseaseService.detectLeafDisease(formData)
         │
         ▼
POST /api/disease/leaf
    Headers: multipart/form-data
    Body: image file
         │
         ▼
Backend processes image
         │
         ▼
Returns:
{
  disease: "Early Blight",
  confidence: "88.4%",
  severity: "Moderate",
  treatment: "Apply fungicide..."
}
         │
         ▼
ResultCard displays result
```

---

## 🎯 Responsive Layout Strategy

### Desktop (> 1024px)
```
┌──────────────────────────────────┐
│  Navbar (Full width)             │
├─────────────┬────────────────────┤
│             │                    │
│   Form      │    Result          │
│   Section   │    Section         │
│   (66%)     │    (33%)           │
│             │                    │
└─────────────┴────────────────────┘
```

### Tablet (640-1024px)
```
┌──────────────────────────────────┐
│  Navbar (Condensed)              │
├─────────────┬────────────────────┤
│             │                    │
│   Form      │    Result          │
│   (60%)     │    (40%)           │
│             │                    │
└─────────────┴────────────────────┘
```

### Mobile (< 640px)
```
┌──────────────────────────────────┐
│  ☰ Navbar (Hamburger)            │
├──────────────────────────────────┤
│                                  │
│    Form Section                  │
│    (100% width)                  │
│                                  │
├──────────────────────────────────┤
│                                  │
│    Result Section                │
│    (100% width)                  │
│                                  │
└──────────────────────────────────┘
```

---

## 🧩 Reusable Components Philosophy

```
┌─────────────────────────────────────┐
│    Atomic Design Principle          │
├─────────────────────────────────────┤
│                                     │
│  Atoms (Basic):                     │
│  ├─ InputField                      │
│  ├─ LoadingSpinner                  │
│  └─ Icon                            │
│                                     │
│  Molecules (Combined):              │
│  ├─ WeatherCard                     │
│  ├─ ResultCard                      │
│  ├─ ImageUploader                   │
│  └─ ChatMessage                     │
│                                     │
│  Organisms (Complex):               │
│  ├─ Navbar                          │
│  ├─ Form Sections                   │
│  └─ Map with Weather                │
│                                     │
│  Templates (Pages):                 │
│  ├─ Dashboard                       │
│  ├─ Module Pages                    │
│  └─ Auth Pages                      │
│                                     │
└─────────────────────────────────────┘
```

---

## 📦 Build & Deployment Flow

```
Development
    │
    ▼
npm run dev
    │
    ▼
Vite Dev Server
    │ (Hot Module Replacement)
    ▼
Test in Browser
    │
    ▼
Ready for Production?
    │
    ▼
npm run build
    │
    ▼
Vite Build Process
    ├─ Minify JS/CSS
    ├─ Optimize images
    ├─ Generate chunks
    └─ Create dist/ folder
    │
    ▼
Deploy to:
    ├─ Netlify (drag & drop dist/)
    ├─ Vercel (Git integration)
    ├─ AWS S3 + CloudFront
    └─ Traditional server (nginx)
```

---

This architecture ensures:
✅ Scalability
✅ Maintainability
✅ Reusability
✅ Performance
✅ Security
✅ Developer Experience

Built with modern best practices! 🚀
